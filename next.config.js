/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["randomuser.me", "lh3.googleusercontent.com", "i.imgur.com", "images.pexels.com"],
      },
}

module.exports = nextConfig
